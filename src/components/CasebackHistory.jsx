import { StyleSheet, Text, View } from "react-native";
import React from "react";

const CasebackHistory = () => {
  return (
    <View>
      <Text>CasebackHistory</Text>
    </View>
  );
};

export default CasebackHistory;

const styles = StyleSheet.create({});
