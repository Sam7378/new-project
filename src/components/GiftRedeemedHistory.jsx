import { StyleSheet, Text, View } from "react-native";
import React from "react";

const GiftRedeemedHistory = () => {
  return (
    <View>
      <Text>GiftRedeemedHistory</Text>
    </View>
  );
};

export default GiftRedeemedHistory;

const styles = StyleSheet.create({});
